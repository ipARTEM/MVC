<Window x:Class="MVC02List.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MVC"
        mc:Ignorable="d"
        Title="MainWindow" Height="700" Width="800" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="250"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0 100 0 0 ">
            <StackPanel>


                <Button Name="bAdd" Content="Add" Margin="2"  Height="20" Click="bAdd_Click" />
                <Button Name="bDelete" Content="Delete" Margin="2"  Height="20" Click="bDelete_Click"  />
            </StackPanel>
            
        </Grid>

        <Grid Grid.Column="1" Margin="2">
            <TextBox x:Name="tbText"  AcceptsReturn="True"
                     ScrollViewer.HorizontalScrollBarVisibility="Auto"
                     ScrollViewer.VerticalScrollBarVisibility="Auto"/>
        </Grid>
       

     

    </Grid>
</Window>
